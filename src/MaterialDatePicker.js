import React from 'react';
/* import {
    DatePicker,
    TimePicker,
    DateTimePicker,
    MuiPickersUtilsProvider,
} from "@material-ui/pickers"; */

class MaterialDatePicker extends React.Component {
/*     constructor(props) {
        super(props);
        this.state = {
            selectedDate: new Date(),
            handleDateChange: new Date(),
        };
        // this.handleChange = this.handleChange.bind(this);
    } */

    render() {

        return (
            {/* <MuiPickersUtilsProvider utils={DateFnsUtils}>
                <DatePicker value={this.state.selectedDate} onChange={this.state.handleDateChange} />
                <TimePicker value={this.state.selectedDate} onChange={this.state.handleDateChange} />
                <DateTimePicker value={this.state.selectedDate} onChange={this.state.handleDateChange} />
            </MuiPickersUtilsProvider> */}

        );
    }
}

export default MaterialDatePicker;